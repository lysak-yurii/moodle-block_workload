# moodle-plugin-ci installation (installed in parent blocks/ directory)
../.moodle-plugin-ci/

# Moodle test installation (only created during full setup)
moodle/
moodledata/

# Local CI helper scripts (CI runs via GitHub Actions)
tests/
phpcs.xml
.moodle-plugin-ci.yml

# Node modules created by local grunt build
moodlenode/

# Local dev/demo utilities – not part of the plugin release
cli/

# moodle-plugin-ci runtime status files (created during local test runs)
Linted
Running

# Raster logo export
pix/logo.png
